[server]
bind = "127.0.0.1:8083"
metrics_bind = "127.0.0.1:9093"
# trusted_proxies = ["10.0.0.0/8"]  # CIDR list of trusted reverse proxies (e.g. ["10.0.0.0/8"]). Required when running behind a load balancer. Omit if directly exposed.

[inspect]
request_timeout_secs = 10
total_timeout_secs = 30
max_redirects = 10
user_agent = "netray-spectra"

[limits]
per_ip_per_minute = 10
per_ip_burst = 5
per_target_per_minute = 30
per_target_burst = 10
max_concurrent_connections = 256

[telemetry]
log_format = "json"  # Override with log_format = "text" for local development
enabled = false
# otlp_endpoint = "http://localhost:4318"
service_name = "spectra"
sample_rate = 1.0

# [ecosystem]
# Public URLs for cross-tool navigation (served to browser frontends).
# ip_base_url = "https://ip.example.com"
# dns_base_url = "https://dns.example.com"
# tls_base_url = "https://tls.example.com"
# http_base_url = "https://http.example.com"
# email_base_url = "https://email.example.com"
# lens_base_url = "https://lens.example.com"

# [backends.ip]
# Internal IP enrichment backend.
# url = "http://ifconfig-rs:8000"
# timeout_ms = 500
# max_concurrent = 20
# cache_ttl_secs = 300
# cache_capacity = 1024
